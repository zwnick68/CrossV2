import { getFighters } from '@lib/mongo/fighters'
import { getBouts } from '@lib/mongo/bouts'
import { getEvents } from '@lib/mongo/events'
// import styles from './page.module.css'



export default async function Fighters() {
  // const events = await getEvents()
  // console.log(events)
  return (
    <main >
    </main>
  )
}